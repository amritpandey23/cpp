#include <iostream>
using namespace std;

struct ListNode
{
    int val;
    ListNode *next;
};

class Solution
{
public:
    bool isPalindrome(ListNode *head)
    {
        if (head->next == NULL)
            return true;

        if (!head)
            return false;
            
    }
};

int main()
{
}
